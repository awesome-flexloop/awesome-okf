---
okf_version: "0.2"
type: Concept
title: "Agent（智能体）— 自主的任务执行者"
description: "Agent 的定义、与 LLM 调用的区别、四大核心能力、工作循环及 SubAgent 模式。"
tags: ["Agent", "智能体", "感知-决策-执行-反思", "SubAgent", "自主执行"]
generated: 2026-09-09
verified: 2026-09-09
status: verified
stale_after: "2027-09-09"
sources:
  - "F-061~F-070"
  - "S2: CSDN 镜像"
---

# 07 Agent（智能体）— 自主的任务执行者

> 对应事实：F-061~F-070
> 知识层级：机制层

---

## 一、什么是 Agent？

**Agent 是具备"感知-决策-执行-反思"循环的 AI 应用架构**，不是新模型，而是新的工程模式。

---

## 二、Agent vs 普通 LLM 调用

```
普通 LLM 调用：
用户问题 → LLM → 回答
（一次往返，单步推理）

Agent 调用：
用户问题 → LLM 规划 → 调用 Tool → 观察结果 → 调整策略 → 调用 Tool → ... → 最终回答
（多步循环，自主决策）
```

---

## 三、Agent 的四大核心能力

| 能力 | 说明 | 示例 |
|------|------|------|
| **感知** | 理解用户意图和环境信息 | 解析"帮我部署项目"的意图 |
| **规划** | 把复杂任务拆解成多个步骤 | 制定：拉代码 → 跑测试 → 构建 → 部署 |
| **执行** | 调用工具、访问 API、操作文件 | 执行 `git pull`、`npm build` |
| **反思** | 评估结果、调整策略、重新规划 | 测试失败 → 分析原因 → 修复代码 |

---

## 四、Agent 的工作循环

```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│  感知   │ →  │  规划   │ →  │  执行   │ →  │  反思   │
│ 理解    │    │ 拆解    │    │ 调用    │    │ 评估    │
│ 意图    │    │ 步骤    │    │ 工具    │    │ 结果    │
└────┬────┘    └────┬────┘    └────┬────┘    └────┬────┘
     │              │              │              │
     │              │         失败时重新规划        │
     │              │              │              │
     └──────────────┴──────────────┴──────────────┘
                          ↓
                       成功时输出
```

---

## 五、SubAgent（子智能体）

Agent 可以调用 SubAgent 来处理子任务，完成后销毁：

```
主 Agent："帮我开发一个用户登录功能"
 │
 ├── SubAgent 1：负责数据库设计
 │    └── 任务完成后销毁
 │
 ├── SubAgent 2：负责 API 开发
 │    └── 任务完成后销毁
 │
 └── SubAgent 3：负责测试用例编写
      └── 任务完成后销毁
```

> **核心认知**：Agent = 感知 + 规划 + 执行 + 反思 的闭环系统。SubAgent 是实现复杂任务并行化的关键手段。
