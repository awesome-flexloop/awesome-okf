# 变更记录

## 2026-09-20

- 按七概念知识沉淀链路 `R -> I -> E -> V` 建立独立 bundle；保留当前微信文章 URL，不覆盖同主题旧 bundle。
- 登记 F-001～F-024；文章四类场景与宣传数字均保留来源距离，官方机制另行核对。
- 判定为技术综述/资讯，非操作教程，不创建 `examples/`。
- 核验发现核心速度、成本、幻觉和候补名单声明缺少独立证据，根 bundle 使用 `status: flagged`。
- 未注册账号、未调用 API、未运行游戏或浏览器演示；Quick Start 仅作为官方契约参考。
- 局部链接检查通过：9 个 Markdown、27 个本地引用全部有效；toctree 与 UTF-8 检查通过。
- 全库计数门禁仅剩并发工作树中的 `sheke/` 计数漂移；本次 AI 分组与总数已按目录树对齐。
- 全 AI 分组扫描存在其他并发 bundle 的历史断链，未改动无关文件；不冒称全库链接通过。
