---
type: Example
title: "PyQt5 系统化学习: 滑动条"
source: "https://www.jianshu.com/p/0883cf04b32b"
generated: { by: "blog-article-to-okf-wiki", at: "2026-09-02T12:00:00+08:00" }
---

# 示例 26：PyQt5 系统化学习: 滑动条

> **信源**：[简书文集《Qt Python GUI 学习（《PyQt5快速开发与实战》笔记）》](https://www.jianshu.com/nb/46416111) · 作者 **水之心** · [原文链接](https://www.jianshu.com/p/0883cf04b32b) · 发布于 2020-06-28
> **对应事实**：F-076 ~ F-078

## 11 QSlider（滑动条）

《PyQt5快速开发与实战》学习笔记。

QSlider 控件提供了一个垂直或水平的滑动条，滑动条是一个用于控制有界值的典型控件，它允许用户沿水平或垂直方向在某一范围内移动滑块，并将滑块所在的位置转换成一个合法范围内的整数值。有时候这种方式比输入数字或者使用 SpinBox （计数器）更加自然。在 slot 函数中对滑块所在位置的处理相当于从整数之间的最小值和最高值进行取值。

一个滑块条控件可以以水平或垂直的方式显示，在构造函数中进行设置。

```python
self.sp=QSlider(Qt.Horizontal) 
self.sp=QSlider(Qt.Vertical) 
```

QSlider 类中的常用方法：

![](/_static/bundles/jishu/gui/pyqt5-gui/images/0883cf04b32b-1114626-c804a8e1a5429186.jpg)

QSlider 类中的常用信号：

![](/_static/bundles/jishu/gui/pyqt5-gui/images/0883cf04b32b-1114626-e088a0d2f1ab1805.jpg)

```python
import sys
from PyQt5.QtCore import *
from PyQt5.QtGui import *
from PyQt5.QtWidgets import *

class SliderDemo(QWidget):
    def __init__(self, parent=None):
        super(SliderDemo, self).__init__(parent)
        self.setWindowTitle("QSlider 例子")
        self.resize(300, 100)

        layout = QVBoxLayout()
        self.l1 = QLabel("Hello PyQt5")
        self.l1.setAlignment(Qt.AlignCenter)
        layout.addWidget(self.l1)
        # 水平方向
        self.sl = QSlider(Qt.Horizontal)
        #设置最小值
        self.sl.setMinimum(10)
        #设置最大值
        self.sl.setMaximum(50)
        # 步长
        self.sl.setSingleStep(3)
        # 设置当前值
        self.sl.setValue(20)
        # 刻度位置，刻度在下方
        self.sl.setTickPosition(QSlider.TicksBelow)
        # 设置刻度间隔
        self.sl.setTickInterval(5)
        layout.addWidget(self.sl)
        # 连接信号槽
        self.sl.valueChanged.connect(self.valuechange)
        self.setLayout(layout)

    def valuechange(self):
        print('current slider value=%s' % self.sl.value())
        size = self.sl.value()
        self.l1.setFont(QFont("Arial", size))

if __name__ == '__main__':
    app = QApplication(sys.argv)
    demo = SliderDemo()
    demo.show()
    sys.exit(app.exec_())
```

![](/_static/bundles/jishu/gui/pyqt5-gui/images/0883cf04b32b-1114626-96974023519bc4db.png)
