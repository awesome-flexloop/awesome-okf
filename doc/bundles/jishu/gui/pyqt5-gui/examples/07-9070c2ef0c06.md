---
type: Example
title: "PyQt5 自定义对话框"
source: "https://www.jianshu.com/p/9070c2ef0c06"
generated: { by: "blog-article-to-okf-wiki", at: "2026-09-02T12:00:00+08:00" }
---

# 示例 07：PyQt5 自定义对话框

> **信源**：[简书文集《Qt Python GUI 学习（《PyQt5快速开发与实战》笔记）》](https://www.jianshu.com/nb/46416111) · 作者 **水之心** · [原文链接](https://www.jianshu.com/p/9070c2ef0c06) · 发布于 2020-07-01
> **对应事实**：F-019 ~ F-021

本例子展示，您输入名字，点击按钮进行问候：

```python
import sys
from PyQt5.QtWidgets import (QLineEdit, QPushButton, QApplication,
    QVBoxLayout, QDialog)

class Form(QDialog):

    def __init__(self, parent=None):
        super().__init__(parent)
        # Create widgets
        self.edit = QLineEdit("此处写下您的名字")
        self.button = QPushButton("展示问候")
        # Create layout and add widgets
        layout = QVBoxLayout()
        layout.addWidget(self.edit)
        layout.addWidget(self.button)
        # Set dialog layout
        self.setLayout(layout)
        # Add button signal to greetings slot
        self.button.clicked.connect(self.greetings)

    # Greets the user
    def greetings(self):
        print ("您好", self.edit.text())

if __name__ == '__main__':
    # Create the Qt Application
    app = QApplication(sys.argv)
    # Create and show the form
    form = Form()
    form.show()
    # Run the main Qt loop
    sys.exit(app.exec_())
```

效果：

![](/_static/bundles/jishu/gui/pyqt5-gui/images/9070c2ef0c06-1114626-cab8e561cd68c0de.png)
