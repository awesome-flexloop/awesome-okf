---
type: Example
title: "Qt5 制作几何画板"
source: "https://www.jianshu.com/p/1b742cafce94"
generated: { by: "blog-article-to-okf-wiki", at: "2026-09-02T12:00:00+08:00" }
---

# 示例 03：Qt5 制作几何画板

> **信源**：[简书文集《Qt for Python（PySide2）官方文档学习笔记》](https://www.jianshu.com/nb/46707335) · 作者 **水之心** · [原文链接](https://www.jianshu.com/p/1b742cafce94) · 发布于 2020-07-01
> **对应事实**：F-007 ~ F-009

参考资料：

- [learnpyqt](https://github.com/learnpyqt) / **[15-minute-apps](https://github.com/learnpyqt/15-minute-apps)**
- [Implementing QPainter flood fill in PyQt5/PySide](https://www.learnpyqt.com/blog/implementing-qpainter-flood-fill-pyqt5pyside/)

## 使用鼠标画矩形

```python
from xinet.Qt.qt5 import QtCore, QtGui, QtWidgets
from xinet.run_qt import run

class MainWindow(QtWidgets.QWidget):
    def __init__(self, *args, **kw):
        super().__init__(*args, **kw)
        self.setWindowTitle("鼠标画矩形")
        self.initUi()

    def initUi(self):
        self.lastPoint = QtCore.QPoint()
        self.endPoint = QtCore.QPoint()
        self.fill_color = QtGui.QColor('white')
        self.brush_color = QtGui.QColor(150, 255, 150, 80)
        self.pix = QtGui.QPixmap(800, 800)
        self.pix.fill(self.fill_color)

    def box_to_center(self, point1, point2):
        x = point1.x()
        y = point1.y()
        w = point2.x() - x
        h = point2.y() - y
        return x, y, w, h

    def paintEvent(self, event):
        painter = QtGui.QPainter(self)
        x, y, w, h = self.box_to_center(self.lastPoint, self.endPoint)
        pp = QtGui.QPainter(self.pix)
        pp.setBrush(self.brush_color)
        pp.drawRect(x, y, w, h)
        painter.drawPixmap(0, 0, self.pix)

    def mousePressEvent(self, event):
        # 鼠标左键按下
        if event.button() == QtCore.Qt.LeftButton:
            self.lastPoint = event.pos()
            self.endPoint = self.lastPoint

    def mouseReleaseEvent(self, event):
        # 鼠标左键释放
        if event.button() == QtCore.Qt.LeftButton:
            self.endPoint = event.pos()
            # 进行重新绘制
            self.update()

if __name__ == "__main__":
    run(MainWindow)
```

效果：

![](/_static/bundles/jishu/gui/qt-for-python/images/1b742cafce94-1114626-c180d481a002edd3.png)

添加控制点：

```python

```
