---
type: Example
title: "Qt 之 QBitmap"
source: "https://www.jianshu.com/p/b1302780e909"
generated: { by: "blog-article-to-okf-wiki", at: "2026-09-02T12:00:00+08:00" }
---

# 示例 24：Qt 之 QBitmap

> **信源**：[简书文集《Qt for Python（PySide2）官方文档学习笔记》](https://www.jianshu.com/nb/46707335) · 作者 **水之心** · [原文链接](https://www.jianshu.com/p/b1302780e909) · 发布于 2020-08-12
> **对应事实**：F-070 ~ F-072

[QBitmap](https://doc.qt.io/qtforpython/PySide2/QtGui/QBitmap.html#qbitmap) 类提供单色（monochrome，1位深度）像素图。

![](/_static/bundles/jishu/gui/qt-for-python/images/b1302780e909-1114626-29505343f6215312.png)

QBitmap类是单色的 off-screen 绘画设备，主要用于创建自定义`QCursor`和`QBrush`对象，构造`QRegion`对象以及为像素图（pixmaps）和小部件（widgets）设置蒙版。

`QBitmap`是确保深度为 1 的`QPixmap`子类，深度为 0 的空对象除外。如果将深度大于 1 的像素图分配给位图，则位图将自动抖动。

在`QBitmap`对象（或深度为 1 的 QPixmap 对象）上绘制时，请使用`QColor`对象`color0`和`color1`。使用`color0`进行绘制会将位图位设置为 0，使用`color1`进行绘制会将位图位设置为 1。对于位图，0 位表示背景（或透明像素），而 1 位表示前景（或不透明像素）。使用`clear()`函数将所有位设置为`color0`。请注意，使用黑色和白色是没有意义的，因为`QColor::pixel()`值对于黑色不一定为 0，对于白色不一定为 1。

`QBitmap`类提供了`transformd()`函数，该函数返回位图的转换后的副本；使用`QTransform`参数平移，缩放，剪切和旋转位图。另外，`QBitmap`提供了静态的`fromData()`函数，该函数返回从给定的`uchar`数据构造的位图，以及静态的`fromImage()`函数，返回转换后的`QImage`对象的副本。

就像`QPixmap`类一样，`QBitmap`通过使用隐式数据共享进行了优化。有关更多信息，请参见[Implicit Data Sharing](https://doc.qt.io/qtforpython/overviews/implicit-sharing.html#implicit-sharing)文档。

![](/_static/bundles/jishu/gui/qt-for-python/images/b1302780e909-1114626-d030c6d796faea1c.png)
